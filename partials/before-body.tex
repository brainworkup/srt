$-- Implements the frontmatter, title page, and abstract.
$--
$-- %%%% TODO %%%%%
$-- Customize is needed, otherwise remove these partials to use Quarto default one
$-- %%%%%%%%%%%%%%%%
$if(has-frontmatter)$
\frontmatter
$endif$
$if(title)$
$if(beamer)$
\frame{\titlepage}
$else$
\maketitle
$endif$
$if(abstract)$
\begin{abstract}
$abstract$
\end{abstract}
$endif$
$endif$
$if(keywords)$
{\bfseries \emph Keywords}
\def\sep{\textbullet\ }
$for(keywords/allbutlast)$$keywords$ \sep $endfor$
$for(keywords/last)$$keywords$$endfor$

$endif$
